spring:

  datasource:
    url: jdbc:postgresql://dev.db-test.apcode.com.br:5433/apadmin-dbname?autoReconnect=true
    username: apadmin-dbuser
    password: apadmin-dbpasswd
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
